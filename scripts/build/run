#!/bin/sh
set -e
lein clean
lein uberjar
result=target/jdi-steprecorder
cat dev-resources/stub.sh target/uberjar/steprecorder-standalone.jar > "$result"
chmod +x "$result"
echo "Created $(realpath "$result")"
